{
    "name": "my-project",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:frontend": "npm install --prefix frontend",
        "install:backend": "cd backend && python -m venv venv && ./venv/bin/pip install -r requirements.txt",
        "install:all": "npm run install:backend && npm run install:frontend",
        "dev": "concurrently \"cd backend && ./venv/bin/python main.py\" \"npm run dev --prefix frontend\"",
        "start:backend": "cd backend && ./venv/bin/python main.py",
        "start:frontend": "npm start --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}